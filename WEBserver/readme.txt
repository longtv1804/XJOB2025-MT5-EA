1. để chạy server:
	+ cài python
	+ cài flask
		$python -m pip install flask
	
2. chạy server:
	2.1 mở command line ở thư mục WEBserver
	2.2 run $python ./server.python
	
3. truy cập server
	vào trình duyệt >> http://localhost:5000//ws?accountID=7777